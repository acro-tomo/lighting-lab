# dependencies
node_modules/

# build / render output (generated)
dist/
output/
*.tsbuildinfo

# tooling scratch
.playwright-cli/

# Instagram サムネイル用フォント（Google Fonts から再取得できるので持たない）
marketing/instagram/fonts/
marketing/instagram/.build/
# セーフゾーン確認用の書き出し（--guides でいつでも作り直せる）
marketing/instagram/out/**/*-guides.png

# local-only Codex agent settings
.agents/
.codex/
AGENTS.md

# local-only Claude Code settings (personal overrides, may contain local paths)
.claude/settings.local.json

# secrets / environment (never commit)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
.idea/
*.swp
