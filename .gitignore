# dependencies
node_modules/

# build / render output (generated)
dist/
output/
*.tsbuildinfo

# tooling scratch
.playwright-cli/

# local-only Claude Code settings (personal overrides, may contain local paths)
.claude/settings.local.json

# secrets / environment (never commit)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
.idea/
*.swp
